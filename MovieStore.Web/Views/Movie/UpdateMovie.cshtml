@model MovieStore.Core.DTOs.UpdateMovieDto

@{
    ViewData["Title"] = "MovieDetail";
}

<h1>Movie Detail</h1>

<h4>MovieDto</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="UpdateMovie">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Title" class="control-label"></label>
                <input asp-for="Title" class="form-control" />
                <span asp-validation-for="Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Year" class="control-label"></label>
                <input asp-for="Year" class="form-control" />
                <span asp-validation-for="Year" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Plot" class="control-label"></label>
                <input asp-for="Plot" class="form-control" />
                <span asp-validation-for="Plot" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label>Genre</label>
                <select asp-for="GenreIds" class="form-control" asp-items="Model.GenreList" multiple>
                    @*@foreach (var item in Model.GenreList)
                    {
                        <option value=item selected="@(Model.Genres.Select(x => x.GenreName).ToList().Contains(item.Text) ? "selected" : null)">@item.Text</option>
                    }*@
                </select>
                <span asp-validation-for="GenreIds" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

